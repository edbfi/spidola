# SPDX-FileCopyrightText: 2026 Spidola contributors
# SPDX-License-Identifier: AGPL-3.0-or-later

name: ci
on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      expected-default-sha:
        description: Exact default-branch commit requested after a checked merge
        type: string
        default: ""
  workflow_call:
permissions:
  contents: read
concurrency:
  group: ci-${{ github.event_name }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true
jobs:
  guard:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - uses: edbfi/automation/actions/dispatch-guard@v2.0.0
        with:
          token: ${{ github.token }}
          expected-default-sha: ${{ inputs.expected-default-sha }}
  core:
    needs: guard
    uses: ./.github/workflows/core.yml
  android:
    needs: guard
    uses: ./.github/workflows/android.yml
  apple:
    needs: guard
    uses: ./.github/workflows/apple.yml
  hygiene:
    needs: guard
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false
      - uses: j178/prek-action@v3.0.0
        with:
          prek-version: 0.5.2
          extra-args: --all-files
        env:
          SKIP: no-commit-to-branch,cargo-fmt,cargo-clippy,check-secret-logging,translations,cargo-deny,swift-format,swiftlint,ktlint,detekt
      - name: Conventional Commits and DCO
        env:
          # CI validates existing commits; the local branch guard rejects main checkouts.
          SKIP: no-commit-to-branch
          PR_BASE: ${{ github.event.pull_request.base.sha }}
          PR_HEAD: ${{ github.event.pull_request.head.sha }}
        run: |
          base="${PR_BASE:-$(git merge-base origin/main HEAD)}"
          head="${PR_HEAD:-$GITHUB_SHA}"
          if [[ "$base" = "$head" ]]; then base="$(git rev-parse "$head^")"; fi
          for commit in $(git rev-list "$base..$head"); do
            git show -s --format=%B "$commit" > "$RUNNER_TEMP/commit-message"
            prek run --hook-stage commit-msg --commit-msg-filename "$RUNNER_TEMP/commit-message"
          done
      - run: git diff --exit-code HEAD
  required:
    name: ci / required
    if: always()
    needs:
      - guard
      - core
      - android
      - apple
      - hygiene
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - uses: edbfi/automation/actions/dispatch-guard@v2.0.0
        with:
          token: ${{ github.token }}
          expected-default-sha: ${{ inputs.expected-default-sha }}
      - uses: edbfi/automation/actions/gate@v2.0.0
        with:
          needs: ${{ toJSON(needs) }}
          required: guard core android apple hygiene
defaults:
  run:
    shell: bash
