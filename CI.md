# Local validation

CI, workflow-based deployment and automated dependency updates are disabled.
Validate changes locally and review updates manually. Use Conventional Commit titles
and matching author sign-offs (`git commit -s`). See the project README and
agent instructions for local commands.
